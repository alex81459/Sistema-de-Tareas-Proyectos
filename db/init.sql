CREATE DATABASE IF NOT EXISTS tareas_proyectos
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;

USE tareas_proyectos;

--las tablas se crean automaticamente via SQLAlchemy Flask-Migrate
--este archivo sirve para asegurar que la BD exista
